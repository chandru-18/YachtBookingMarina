<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Yacht: <%= yacht.name %> - Admin</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <header>
        <h1>Admin Dashboard</h1>
        <p>Manage users and bookings</p>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
                <li><a href="/dashboard">Dashboard</a></li>
                <li><a href="/bookings">My Bookings</a></li>
                <li><a href="/profile">Profile</a></li>
                <li><a href="/admin/dashboard">Admin Panel</a></li>
                <li><a href="/logout">Logout</a></li>
            </ul>
        </nav>
    </header>

    <main>
        <div class="message-container">
            <% if (messages.success && messages.success.length > 0) { %>
                <div class="message success"><%= messages.success[0] %></div>
            <% } %>
            <% if (messages.error && messages.error.length > 0) { %>
                <div class="message error"><%= messages.error[0] %></div>
            <% } %>
        </div>

        <section class="form-container">
            <h2>✏️ Edit <%= yacht.display || yacht.name %></h2>
            <img src="<%= yacht.imageUrl %>" alt="<%= yacht.name %>" style="max-width: 150px; height: auto; border-radius: 8px; margin-bottom: 20px;">

            <form action="/admin/yachts/edit/<%= yacht._id %>" method="POST" enctype="multipart/form-data" class="styled-form">
                <label for="name">Yacht Name (Unique)</label>
                <input type="text" id="name" name="name" value="<%= yacht.name %>" required>

                <label for="display">Display Name</label>
                <input type="text" id="display" name="display" value="<%= yacht.display %>">

                <label for="maxPersons">Max Persons</label>
                <input type="number" id="maxPersons" name="maxPersons" min="1" value="<%= yacht.maxPersons %>" required>

                <label for="pricePerHour">Price Per Hour (AED)</label>
                <input type="number" id="pricePerHour" name="pricePerHour" step="0.01" min="0" value="<%= yacht.pricePerHour %>" required>

                <label for="description">Description</label>
                <textarea id="description" name="description" rows="5"><%= yacht.description %></textarea>

                <label for="availability">Availability Status</label>
                <select id="availability" name="availability">
                    <option value="true" <%= yacht.availability ? 'selected' : '' %>>Available</option>
                    <option value="false" <%= !yacht.availability ? 'selected' : '' %>>Unavailable</option>
                </select>

                <label for="imageUrl">Change Yacht Image</label>
                <input type="file" id="imageUrl" name="imageUrl" accept="image/jpeg, image/png, image/gif">
                <p style="font-size: 0.8em; color: #777; margin-top: -10px;">(Leave empty to keep current image. Max 5MB, JPG/PNG/GIF.)</p>

                <button type="submit" class="btn btn-primary">Update Yacht</button>
                <a href="/admin/yachts" class="btn btn-secondary" style="margin-top: 10px;">Cancel</a>
            </form>
        </section>
    </main>

    <footer>
        <p>&copy; <%= new Date().getFullYear() %> Yacht Marina Booking. All rights reserved.</p>
    </footer>
</body>
</html>