<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Yacht - Admin</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css"> <style>
        .current-images img {
            max-width: 100px;
            height: auto;
            margin: 5px;
            border: 1px solid #ddd;
            padding: 2px;
            border-radius: 4px;
        }
    </style>
</head>
<body>
    <%- include('/partials/header') %>

    <div class="container mt-5">
        <h2 class="mb-4 text-center">Edit Yacht: <%= yacht.name %></h2>

        <%- include('../partials/flash_messages') %>

        <div class="card p-4 mx-auto" style="max-width: 700px;">
            <form action="/admin/yachts/edit/<%= yacht._id %>?_method=POST" method="POST" enctype="multipart/form-data">
                <div class="mb-3">
                    <label for="name" class="form-label">Yacht Name</label>
                    <input type="text" class="form-control" id="name" name="name" value="<%= yacht.name %>" required>
                </div>
                <div class="mb-3">
                    <label for="display" class="form-label">Display Name (Optional)</label>
                    <input type="text" class="form-control" id="display" name="display" value="<%= yacht.display || '' %>">
                </div>
                <div class="mb-3">
                    <label for="maxPersons" class="form-label">Max Persons</label>
                    <input type="number" class="form-control" id="maxPersons" name="maxPersons" min="1" value="<%= yacht.maxPersons %>" required>
                </div>
                <div class="mb-3">
                    <label for="pricePerHour" class="form-label">Price Per Hour (₹)</label>
                    <input type="number" class="form-control" id="pricePerHour" name="pricePerHour" min="0" step="0.01" value="<%= yacht.pricePerHour %>" required>
                </div>
                <div class="mb-3">
                    <label for="description" class="form-label">Description</label>
                    <textarea class="form-control" id="description" name="description" rows="3"><%= yacht.description %></textarea>
                </div>

                <div class="mb-3">
                    <label class="form-label">Current Images:</label>
                    <div class="current-images d-flex flex-wrap">
                        <% if (yacht.imageUrls && yacht.imageUrls.length > 0) { %>
                            <% yacht.imageUrls.forEach(url => { %>
                                <img src="<%= url %>" alt="Current Yacht Image">
                            <% }); %>
                        <% } else { %>
                            <p class="text-muted">No images currently uploaded.</p>
                        <% } %>
                    </div>
                    <small class="form-text text-muted">Uploading new images will replace ALL current images.</small>
                </div>

                <div class="mb-3">
                    <label for="images" class="form-label">Upload New Images (up to 5)</label>
                    <input type="file" class="form-control" id="images" name="images" accept="image/*" multiple>
                    <small class="form-text text-muted">Select new images to replace existing ones. (JPG, JPEG, PNG, GIF)</small>
                </div>

                <div class="mb-3 form-check form-switch">
                    <input class="form-check-input" type="checkbox" id="availability" name="availability" value="true" <%= yacht.availability ? 'checked' : '' %>>
                    <label class="form-check-label" for="availability">Available for Booking</label>
                </div>

                <div class="d-grid gap-2 mt-4">
                    <button type="submit" class="btn btn-success">Update Yacht</button>
                    <a href="/admin/yachts" class="btn btn-secondary">Cancel</a>
                </div>
            </form>
        </div>
    </div>

    <%- include('../partials/footer') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>